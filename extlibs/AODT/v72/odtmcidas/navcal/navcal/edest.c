/* edest.f -- translated by f2c (version 19990503).
   You must link the resulting object file with the libraries:
	-lf2c -lm   (in that order)
*/

#include "f2c.h"

/* Subroutine */ int edestX_(char *ctext, integer *ival, ftnlen ctext_len)
{
    /* Builtin functions */
    integer i_len(char *, ftnlen);

    /* Local variables */
    static integer ilen;

    ilen = i_len(ctext, ctext_len);
/* 	write(*,*) ctext(1:ilen), ival */
    return 0;
} /* edest_ */

